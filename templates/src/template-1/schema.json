{
  "$schema": "http://json-schema.org/schema",
  "$id": "CustomNgNew",
  "title": "customized ng new schematic",
  "type": "object",
  "description": "Initialize a new project",
  "required": ["name"],
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the project",
      "x-prompt": "Qual o tema do template selecionado?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  }
}
